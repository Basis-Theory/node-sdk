// This file was auto-generated by Fern from our API Definition.

import * as BasisTheory from "../../../../src/api/index";
import { BasisTheoryClient } from "../../../../src/Client";
import { mockServerPool } from "../../../mock-server/MockServerPool";

describe("CredentialsClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: [
                {
                    id: "cred_abc123",
                    allowance_id: "allowance_id",
                    payment_method_id: "payment_method_id",
                    rail: "rail",
                    provider: "vic",
                    format: "card",
                    provider_ids: { key: "value" },
                    amount: { value: "100.00", currency: "USD" },
                    merchant: { name: "name", url: "url", country_code: "country_code" },
                    status: "created",
                    expires_at: "2024-01-15T09:30:00Z",
                    created_at: "2024-01-15T09:30:00Z",
                },
            ],
            pagination: { next: "next", page_size: 1 },
        };

        server
            .mockEndpoint({ once: false })
            .get("/agentic/allowances/allowance_id/credentials")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            data: [
                {
                    id: "cred_abc123",
                    allowanceId: "allowance_id",
                    paymentMethodId: "payment_method_id",
                    rail: "rail",
                    provider: "vic",
                    format: "card",
                    providerIds: {
                        key: "value",
                    },
                    amount: {
                        value: "100.00",
                        currency: "USD",
                    },
                    merchant: {
                        name: "name",
                        url: "url",
                        countryCode: "country_code",
                    },
                    status: "created",
                    expiresAt: new Date("2024-01-15T09:30:00.000Z"),
                    createdAt: new Date("2024-01-15T09:30:00.000Z"),
                },
            ],
            pagination: {
                next: "next",
                pageSize: 1,
            },
        };
        const page = await client.agentic.allowances.credentials.list("allowance_id", {
            size: 1,
            start: "start",
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/agentic/allowances/allowance_id/credentials")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.allowances.credentials.list("allowance_id");
        }).rejects.toThrow(BasisTheory.ForbiddenError);
    });

    test("list (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/agentic/allowances/allowance_id/credentials")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.allowances.credentials.list("allowance_id");
        }).rejects.toThrow(BasisTheory.NotFoundError);
    });

    test("create (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { rail: "agentic-token", provider: "vic", credential: { format: "card" } };
        const rawResponseBody = {
            id: "cred_abc123",
            rail: "rail",
            provider: "vic",
            amount: { value: "100.00", currency: "USD" },
            credential: { format: "card", value: { key: "value" } },
            provider_ids: { key: "value" },
            expires_at: "2024-01-15T09:30:00Z",
        };

        server
            .mockEndpoint()
            .post("/agentic/allowances/allowance_id/credentials")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.agentic.allowances.credentials.create("allowance_id", {
            rail: "agentic-token",
            provider: "vic",
            credential: {
                format: "card",
            },
        });
        expect(response).toEqual({
            id: "cred_abc123",
            rail: "rail",
            provider: "vic",
            amount: {
                value: "100.00",
                currency: "USD",
            },
            credential: {
                format: "card",
                value: {
                    key: "value",
                },
            },
            providerIds: {
                key: "value",
            },
            expiresAt: new Date("2024-01-15T09:30:00.000Z"),
        });
    });

    test("create (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { rail: "agentic-token", provider: "vic", credential: { format: "card" } };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/agentic/allowances/allowance_id/credentials")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.allowances.credentials.create("allowance_id", {
                rail: "agentic-token",
                provider: "vic",
                credential: {
                    format: "card",
                },
            });
        }).rejects.toThrow(BasisTheory.BadRequestError);
    });

    test("create (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { rail: "agentic-token", provider: "vic", credential: { format: "card" } };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/agentic/allowances/allowance_id/credentials")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.allowances.credentials.create("allowance_id", {
                rail: "agentic-token",
                provider: "vic",
                credential: {
                    format: "card",
                },
            });
        }).rejects.toThrow(BasisTheory.ForbiddenError);
    });

    test("create (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { rail: "agentic-token", provider: "vic", credential: { format: "card" } };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/agentic/allowances/allowance_id/credentials")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.allowances.credentials.create("allowance_id", {
                rail: "agentic-token",
                provider: "vic",
                credential: {
                    format: "card",
                },
            });
        }).rejects.toThrow(BasisTheory.NotFoundError);
    });

    test("create (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });
        const rawRequestBody = { rail: "agentic-token", provider: "vic", credential: { format: "card" } };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/agentic/allowances/allowance_id/credentials")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.allowances.credentials.create("allowance_id", {
                rail: "agentic-token",
                provider: "vic",
                credential: {
                    format: "card",
                },
            });
        }).rejects.toThrow(BasisTheory.ConflictError);
    });

    test("get (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            id: "cred_abc123",
            allowance_id: "allowance_id",
            payment_method_id: "payment_method_id",
            rail: "rail",
            provider: "vic",
            format: "card",
            provider_ids: { key: "value" },
            amount: { value: "100.00", currency: "USD" },
            merchant: {
                name: "name",
                url: "url",
                country_code: "country_code",
                category_code: "category_code",
                acquirer_bin: "acquirer_bin",
            },
            status: "created",
            expires_at: "2024-01-15T09:30:00Z",
            created_at: "2024-01-15T09:30:00Z",
        };

        server
            .mockEndpoint()
            .get("/agentic/allowances/allowance_id/credentials/credential_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.agentic.allowances.credentials.get("allowance_id", "credential_id");
        expect(response).toEqual({
            id: "cred_abc123",
            allowanceId: "allowance_id",
            paymentMethodId: "payment_method_id",
            rail: "rail",
            provider: "vic",
            format: "card",
            providerIds: {
                key: "value",
            },
            amount: {
                value: "100.00",
                currency: "USD",
            },
            merchant: {
                name: "name",
                url: "url",
                countryCode: "country_code",
                categoryCode: "category_code",
                acquirerBin: "acquirer_bin",
            },
            status: "created",
            expiresAt: new Date("2024-01-15T09:30:00.000Z"),
            createdAt: new Date("2024-01-15T09:30:00.000Z"),
        });
    });

    test("get (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/agentic/allowances/allowance_id/credentials/credential_id")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.allowances.credentials.get("allowance_id", "credential_id");
        }).rejects.toThrow(BasisTheory.ForbiddenError);
    });

    test("get (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/agentic/allowances/allowance_id/credentials/credential_id")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.allowances.credentials.get("allowance_id", "credential_id");
        }).rejects.toThrow(BasisTheory.NotFoundError);
    });
});
