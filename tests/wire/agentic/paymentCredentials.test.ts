// This file was auto-generated by Fern from our API Definition.

import * as BasisTheory from "../../../src/api/index";
import { BasisTheoryClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("PaymentCredentialsClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: [
                {
                    id: "cred_abc123",
                    allowance_id: "allowance_id",
                    payment_method_id: "payment_method_id",
                    rail: "rail",
                    provider: "vic",
                    format: "card",
                    provider_ids: { key: "value" },
                    amount: { value: "100.00", currency: "USD" },
                    merchant: { name: "name", url: "url", country_code: "country_code" },
                    status: "created",
                    expires_at: "2024-01-15T09:30:00Z",
                    created_at: "2024-01-15T09:30:00Z",
                },
            ],
            pagination: { next: "next", page_size: 1 },
        };

        server
            .mockEndpoint({ once: false })
            .get("/agentic/payment-credentials")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            data: [
                {
                    id: "cred_abc123",
                    allowanceId: "allowance_id",
                    paymentMethodId: "payment_method_id",
                    rail: "rail",
                    provider: "vic",
                    format: "card",
                    providerIds: {
                        key: "value",
                    },
                    amount: {
                        value: "100.00",
                        currency: "USD",
                    },
                    merchant: {
                        name: "name",
                        url: "url",
                        countryCode: "country_code",
                    },
                    status: "created",
                    expiresAt: new Date("2024-01-15T09:30:00.000Z"),
                    createdAt: new Date("2024-01-15T09:30:00.000Z"),
                },
            ],
            pagination: {
                next: "next",
                pageSize: 1,
            },
        };
        const page = await client.agentic.paymentCredentials.list({
            size: 1,
            start: "start",
        });

        expect(expected.data).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.data).toEqual(nextPage.data);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({
            maxRetries: 0,
            apiKey: "test",
            correlationId: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/agentic/payment-credentials")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.paymentCredentials.list();
        }).rejects.toThrow(BasisTheory.ForbiddenError);
    });
});
