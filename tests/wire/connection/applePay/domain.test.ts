/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { mockServerPool } from "../../../mock-server/MockServerPool";
import { BasisTheoryClient } from "../../../../src/Client";
import * as BasisTheory from "../../../../src/api/index";

describe("Domain", () => {
    test("deregister", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({ apiKey: "test", correlationId: "test", environment: server.baseUrl });
        const rawRequestBody = { domain: "domain" };

        server
            .mockEndpoint()
            .post("/connections/apple-pay/domain-deregistration")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.connection.applePay.domain.deregister({
            domain: "domain",
        });
        expect(response).toEqual(undefined);
    });

    test("get", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({ apiKey: "test", correlationId: "test", environment: server.baseUrl });

        const rawResponseBody = { domains: [{ domain: "domain", status: "status" }] };
        server
            .mockEndpoint()
            .get("/connections/apple-pay/domain-registration")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.connection.applePay.domain.get();
        expect(response).toEqual({
            domains: [
                {
                    domain: "domain",
                    status: "status",
                },
            ],
        });
    });

    test("register", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({ apiKey: "test", correlationId: "test", environment: server.baseUrl });
        const rawRequestBody = { domain: "domain" };
        const rawResponseBody = { domains: [{ domain: "domain", status: "status" }] };
        server
            .mockEndpoint()
            .post("/connections/apple-pay/domain-registration")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.connection.applePay.domain.register({
            domain: "domain",
        });
        expect(response).toEqual({
            domains: [
                {
                    domain: "domain",
                    status: "status",
                },
            ],
        });
    });

    test("registerAll", async () => {
        const server = mockServerPool.createServer();
        const client = new BasisTheoryClient({ apiKey: "test", correlationId: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = { domains: [{ domain: "domain", status: "status" }] };
        server
            .mockEndpoint()
            .put("/connections/apple-pay/domain-registration")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.connection.applePay.domain.registerAll({});
        expect(response).toEqual({
            domains: [
                {
                    domain: "domain",
                    status: "status",
                },
            ],
        });
    });
});
