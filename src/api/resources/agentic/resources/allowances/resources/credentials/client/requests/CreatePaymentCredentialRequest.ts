// This file was auto-generated by Fern from our API Definition.

import type * as BasisTheory from "../../../../../../../../index.js";

/**
 * @example
 *     {
 *         rail: "agentic-token",
 *         provider: "vic",
 *         credential: {
 *             format: "card"
 *         }
 *     }
 */
export interface CreatePaymentCredentialRequest {
    /** Optional stable key for detecting retries. A successful bearer credential cannot be replayed. */
    btIdempotencyKey?: string;
    rail: BasisTheory.agentic.allowances.CreatePaymentCredentialRequestRail;
    provider: BasisTheory.agentic.allowances.CreatePaymentCredentialRequestProvider;
    amount?: BasisTheory.SharedPaymentAmount;
    /** Merchant this credential is being minted for. Required when the allowance has no `merchant`, and rejected when it does — the allowance's merchant is the scope the cardholder verified against, so a mint can neither restate nor replace it. Required on every rail for consistency. At mint Visa (`vic`) forwards it to the network, and Stripe Link (`link`) names it on the spend request the consumer sees. */
    merchant?: BasisTheory.SharedPaymentMerchant;
    credential: BasisTheory.agentic.allowances.CreatePaymentCredentialRequestCredential;
}
