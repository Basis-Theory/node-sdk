// This file was auto-generated by Fern from our API Definition.

import type * as BasisTheory from "../index.js";

/**
 * Each action is only valid for the providers that run that ceremony: Visa (`vic`) uses the session/OTP/passkey actions, Mastercard (`agentpay`) uses `complete`, and an interactive Stripe Link allowance (`virtual-card`/`link`) uses `start` to obtain the consumer's approval redirect and `complete` to check it once. A rail with no cardholder ceremony (`spt`, or a delegated `virtual-card` allowance) accepts `start` and answers `400 VERIFICATION_NOT_REQUIRED`.
 */
export type VerifyAllowanceRequest =
    | BasisTheory.VerifyAllowanceRequest.Start
    | BasisTheory.VerifyAllowanceRequest.SubmitSession
    | BasisTheory.VerifyAllowanceRequest.Complete
    | BasisTheory.VerifyAllowanceRequest.SelectOtpMethod
    | BasisTheory.VerifyAllowanceRequest.SubmitOtp
    | BasisTheory.VerifyAllowanceRequest.SubmitPasskey;

export namespace VerifyAllowanceRequest {
    export interface Start extends BasisTheory.VerifyAllowanceRequestStart {
        action: "start";
    }

    export interface SubmitSession extends BasisTheory.VerifyAllowanceRequestSubmitSession {
        action: "submit_session";
    }

    export interface Complete extends BasisTheory.VerifyAllowanceRequestComplete {
        action: "complete";
    }

    export interface SelectOtpMethod extends BasisTheory.VerifyAllowanceRequestSelectOtpMethod {
        action: "select_otp_method";
    }

    export interface SubmitOtp extends BasisTheory.VerifyAllowanceRequestSubmitOtp {
        action: "submit_otp";
    }

    export interface SubmitPasskey extends BasisTheory.VerifyAllowanceRequestSubmitPasskey {
        action: "submit_passkey";
    }
}
